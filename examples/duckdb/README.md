---
tools:
  - [duckdb, -c, { regex: "^SELECT\\b.*" }]
---

# Instructions
- Understand the schema by exploring the parquet files in ./data
- Translate the user's question into a query that answers it
